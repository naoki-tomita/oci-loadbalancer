version: '3'
services:
  load-balancer:
    image: nginx:latest
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 8080:8080
  nzxt-docs:
    image: ghcr.io/naoki-tomita/nzxt-docs:latest
    environment:
      - PORT=80
